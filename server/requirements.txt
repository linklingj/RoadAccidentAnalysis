flask>=3.0
flask-cors>=4.0
gunicorn>=21.2
# Inference pipeline dependencies (already present in `dl` conda env, listed here for Docker/EC2)
opencv-python-headless>=4.9
numpy>=1.26
torch>=2.2
ultralytics>=8.3
# PerspectiveFields ships in the repo's PerspectiveFields/ folder; install editable inside the image
